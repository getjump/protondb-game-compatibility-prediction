# ML Pipeline — план улучшений (фаза 2)

## Текущие результаты

| Метрика | Значение |
|---------|----------|
| Accuracy | 0.6953 |
| F1 macro | 0.5861 |
| Features | 99 |
| Гиперпараметры | n_est=2000, lr=0.03, class_weight={0:2, 1:1, 2:3} |

---

## Прогресс экспериментов

| Шаг | Accuracy | F1 macro |
|-----|----------|----------|
| Baseline (no leakage) | 0.7246 | 0.5028 |
| + driver split | 0.7239 | 0.5010 |
| + class balancing | 0.6433 | 0.5526 |
| + game_metadata | 0.6529 | 0.5440 |
| + report_age_days | 0.6799 | 0.5553 |
| **+ variant** | **0.6953** | **0.5861** |

---

## План

### 1. ✅ Добавить `variant` как фичу

**Результат: Accuracy 0.6953, F1 macro 0.5861 (+0.03)**

Вошла в SHAP top-3 (0.3103). works_oob recall 0.56 (был 0.44). Лучший результат.

### 2. ❌ Добавить `cpu_family` как фичу

**Результат: нет улучшения (F1 macro 0.5844, -0.0017). Откатили.**

CPU embeddings уже кодируют семейства через SVD — явная категория не добавляет сигнала.

### 3. ❌ Увеличить `n_estimators` до 4000

**Результат: без изменений (F1 macro 0.5860). Early stopping на итерации 2090.**

Модель сконвергировала — 2000 достаточно.

### 4. ⏳ Deck-specific фичи (`deck_status`)

Пропущено — steam enrichment не завершён, `deck_status` пуст (0% покрытие).

### 5. ❌ Тюнинг гиперпараметров (`num_leaves`, `min_child_samples`)

**Результат: нет улучшения. Grid search по 6 конфигурациям — разброс ±0.003 F1.**

Текущие 63/20 оптимальны.